[project]
name = "ag2-examples"
version = "0.1.0"
description = "AG2 framework examples"
requires-python = ">=3.12"
dependencies = [
    "ag2[openai,mcp,a2a]",
    "mcp",
    "pydantic",
    "pydantic-settings",
    "python-dotenv",
    "uvicorn",
]
